{% extends 'AntfrogerTravelWithMeBundle:Layout:layout.html.twig' %}

{% block content %}
    <h1 class="title">Login</h1>

    <p>
        Choose between default users: <em>antoine/antoine</em> <small>(ROLE_USER)</small>, <em>admin/admin</em> <small>(ROLE_ADMIN)</small> or <em>superadmin/superadmin</em> <small>(ROLE_SUPER_ADMIN)</small>
    </p>

    {% if error %}
        <div class="error">{{ error.message }}</div>
    {% endif %}

    <form action="{{ path("_twm_security_check") }}" method="post" id="login">
        <div>
            <label for="username">Username</label>
            <input type="text" id="username" name="_username" value="{{ last_username }}" />
        </div>

        <div>
            <label for="password">Password</label>
            <input type="password" id="password" name="_password" />
        </div>

        <button type="submit" class="sf-button">
            <span class="border-l">
                <span class="border-r">
                    <span class="btn-bg">Login</span>
                </span>
            </span>
        </button>
    </form>
{% endblock %}

{#% set code = code(_self) %#}
